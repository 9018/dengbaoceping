# Legacy 目录说明

该目录用于承接旧仓库遗留资产，不作为当前主链路运行时目录。

- `runtime/`：切换前 root 级运行时目录的归档备份
- `knowledge/`：旧 `rules/`、`templates/` 等知识沉淀
- `deploy/`：旧部署经验或脚本归档
- `cutover/`：切换过程补充材料
- `rollback/`：回滚过程补充材料

主链路仍以 `backend/uploads`、`backend/exports`、`backend/snapshots` 与 `backend/app/rules` 为准。
